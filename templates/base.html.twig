<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    {# Título principal de la web, lo personalizo por página si lo deseo con block title #}
    <title>{% block title %}CONSORIA Inmobiliaria{% endblock %}</title>
    {# Aquí más adelante irán los links de CSS cuando pase al diseño visual #}
</head>
<body>
    {# === MENÚ DE NAVEGACIÓN PRINCIPAL === 
       Siempre visible arriba en todas las páginas (lo repito usando este base)
    #}
    <nav>
        {# El logo de la agencia (o solo el nombre si no tengo logo aún),
           siempre me lleva a la página principal (Home) #}
        <a href="{{ path('home') }}">
    <img src="assets/img/logo.png" alt="Logo CONSORIA Inmobiliaria" height="80" style="vertical-align: middle;">
    <span style="font-weight:bold; font-size:18px; vertical-align: middle;">CONSORIA Inmobiliaria</span>
</a>


        {# === Menú principal ===
           Estas rutas son las esenciales según el REFERENTIEL y la práctica del sector
           Siempre reviso que los paths coincidan con mis rutas Symfony #}
        <a href="{{ path('home') }}">Inicio</a>
        <a href="{{ path('buy') }}">Comprar</a>
        <a href="{{ path('sell') }}">Vender</a>
        <a href="{{ path('rent') }}">Arriendo/Vacacional</a>
        <a href="{{ path('about') }}">Nosotros</a>
        <a href="{{ path('contact') }}">Contacto</a>
        {# Si más adelante necesito login, dashboard, etc., agrego aquí #}
    </nav>

    {# Aquí puedo poner los mensajes flash (éxito, error, etc.) si lo necesito más adelante #}

    {# === CONTENIDO PRINCIPAL ===
       Cada página extiende este archivo y pone su contenido en block body #}
    <div class="container">
        {% block body %}{% endblock %}
    </div>

    {# === FOOTER ===
       Pie de página fijo y sencillo, siempre presente
       Más adelante lo puedo hacer responsivo, agregar más enlaces, etc. #}
    <footer>
        <div>
            <strong>Oficina en Bogotá - Colombia</strong>
            <p>Carrera 7 #85-24, Chapinero</p>
        </div>
        <div>
            <strong>Oficina en París - Francia</strong>
            <p>12 Rue de Paris, 75000 Paris</p>
        </div>
        <div>
            <strong>Habla con un agente</strong>
            <p><a href="#">WhatsApp</a></p>
        </div>
    </footer>
</body>
</html>
